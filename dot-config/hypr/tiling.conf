# Master layout related bindings.
bind = $mainMod, Return, layoutmsg, focusmaster master
bind = $mainMod SHIFT, Return, layoutmsg, swapwithmaster master
bind = $mainMod, J, layoutmsg, cyclenext
bind = $mainMod, K, layoutmsg, cycleprev
bind = $mainMod SHIFT, J, layoutmsg, swapnext
bind = $mainMod SHIFT, K, layoutmsg, swapprev
bind = $mainMod, V, togglefloating,
### RESIZE ###
bind = $mainMod, r, submap, resize
submap = resize
# sets repeatable binds for resizing the active window
binde =,L,resizeactive,50 0
binde =,H,resizeactive,-50 0
binde =,K,resizeactive,0 -50
binde =,J,resizeactive,0 50
# Master layout orientation
bind =,U, layoutmsg, orientationleft
bind =,O, layoutmsg, orientationright
bind =,B, layoutmsg, orientationbottom
bind =,T, layoutmsg, orientationtop
bind =,C, layoutmsg, orientationcenter
# use reset to go back to the global submap
bind=,escape,submap,reset 
# will reset the submap, which will return to the global submap
submap=reset

## Workspaces ###
bindd = $mainMod, L, Next workspace, workspace, e+1
bindd = $mainMod, H, Previous workspace, workspace, e-1
# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Cycle through applications on active workspace
bindd = $mainMod, Tab, Cycle to next window, cyclenext
bindd = $mainMod SHIFT, Tab, Cycle to prev window, cyclenext, prev
bindd = $mainMod, Tab, Reveal active window on top, bringactivetotop
bindd = $mainMod SHIFT, Tab, Reveal active window on top, bringactivetotop

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
